import React from "react";
import { ClockLoader } from "react-spinners";
import { COLOR_PALETTE } from "./color-palette";

const Loading = () => <ClockLoader color={COLOR_PALETTE.color4} size={65} />;

export default Loading;
